#!/bin/sh

if [ -z "$1" ]; then
	echo 1>&2 "usage: bak file"
	echo 1>&2 "bak requires one or more file"
	exit 1
fi

for f do
	[ -f $f ] || { echo 1>&2 "$f: no exist"; continue; }
	cp -f $f $f.bak
	if [ -p /dev/stdout ]; then
		echo $f.bak
	fi
done

exit 0
