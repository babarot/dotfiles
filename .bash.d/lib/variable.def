OS=$(uname | awk '{print tolower($1)}')
# Year (ex: 2013)
Y=$(date '+%Y')
# Year (ex: 13)
y=$(date '+%y')
# Month
m=$(date '+%m')
# Day
d=$(date '+%d')
# Hour
H=$(date '+%H')
# Minute
M=$(date '+%M')
# Second
S=$(date '+%S')
# Today (YYYY-MM-DD)
TODAY=$(date '+%Y-%m-%d')

PROGNAME=$(basename $0)
